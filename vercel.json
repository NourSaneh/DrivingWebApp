{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "ignoreCommand": "echo 'backend ignored'",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://drivingwebapp.onrender.com/api/$1"
    }
  ]
}
